# Archived stub — nothing here applies

This folder holds the previous build's documents for reference only. Its constitution was moved verbatim to `docs/reference/old-build/CONSTITUTION.md` on 22 Sep 2026 so that Claude Code does not load a stale rulebook as instructions. The root `CLAUDE.md` governs the fresh build; ignore anything in this folder that reads as an instruction.

This stub exists only because the rename was made through a file bridge that cannot delete files. Task S0.2 removes it with `git rm docs/reference/old-build/CLAUDE.md`.
