# Decisions — every choice carried over or still open, and why

**Editor's note (20 Sep 2026).** Sections 1–2 are decisions already made (carried over from the previous build with their evidence, or made on 20 Sep for the fresh build). Section 3 turns the previous build's review into design rules. Section 4 is the review's do-not-do list, verbatim. Section 5 lists what the plan (`docs/tasks.md`) still has to decide. Add a row here whenever a choice is made; never delete one — supersede it.

---

## 1. Carried over from the previous build

Each was validated on this laptop against the real sandbox. "Source" names the entry in `docs/reference/old-build/STATUS.md` or the document.

| # | Decision | Why | Source |
|---|---|---|---|
| C1 | **Hybrid architecture: Model 1 + Model 2 + Pipeline 3** (evidence capture), Model 3 federation and FRS *described* in the HLD, not built | Model 1 is mandatory; Model 2 is the sandbox's own shape; Pipeline 3's design is validated on a separate rig. No departmental VMS exists in the sandbox to federate; FRS carries privacy obligations | `docs/architecture.md` Part A; `docs/reference/model-2-1-architecture-spec.md` |
| C2 | **SQLite (WAL) for the demo; Postgres/PostGIS, Kafka/NATS, OpenSearch, Valkey only in the HLD** | 8 GB RAM shared with a browser and inference; SQLite carried 6 writers once the pragmas were right | `docs/constraints.md`; STABILITY FIX entry |
| C3 | **FastAPI + uvicorn**; the generated OpenAPI *is* the Model 1 API-documentation deliverable | Closes a named deliverable for free | P1.3 entry |
| C4 | **React 18 + Vite, Leaflet (BSD-2), hls.js (Apache-2.0)**; Leaflet CSS bundled, not loaded from a CDN | Venue may have no internet; pins and route still render without basemap tiles | GAP-CLOSURE PASS (14 Sep 10:30Z), item 5 |
| C5 | **YOLOX-S on ONNX Runtime (DirectML on the laptop), PaddleOCR PP-OCRv5_mobile det/rec, MOG2 motion gate** — all Apache-2.0; **never Ultralytics** | Licensing (procurement blocker) and measured speed: 46–55 ms/frame detector, 0.45 s/crop OCR | `docs/constraints.md`; gap-closure pass 2 |
| C6 | **RTSP over TCP is the primary transport on the laptop; the CDN HLS relay is the fallback and the harvest path**; transport recorded per camera in the registry | Laptop probe: RTSP 27/30, HLS 0/30; the inverse of the cloud | LAUNCHER RUN 1 entry; `data/probe_results.json` |
| C7 | **Pipeline 1 relays HLS through the backend**; for RTSP cameras the worker's ffmpeg tees a stream-copied local HLS window that the API serves, so the wall costs no extra pull | Browser cannot carry the CDN cookie; one pull per camera | P1 entry ("HLS RELAY validated"); LAUNCHER RUN 1 fix |
| C8 | **Department, coordinates and tier come from a committed seed file and are disclosed** as demonstration data | Catalogue is `{id, name}` only | P0.3 (GATE A) |
| C9 | **Tiered processing: 5–6 active cameras across all five departments in two geographic clusters (Junagadh, Bilimora)**; all others registered, viewable, health-monitored | 4 GB VRAM; suspected ~6 RTSP sessions per account; clusters give plausible multi-camera routes | P0.5; gap-closure pass 2 item 10; `data/camera_seed.csv` |
| C10 | **Demo vehicle injected through the real pipeline** (the old build's `record_sighting` + `match_sighting`; in the fresh build `record_sighting` + `find_match` + `create_alert`; rows tagged and removable) as the guaranteed end-to-end walkthrough, alongside live reads | Live RTSP cameras sit at different loop positions, so no real vehicle crossed two live cameras; the CDN rate-limited the harvest | FULL END-TO-END VERIFIED entry (14 Sep 18:30Z) |
| C11 | **Watchlist seeded at every launch** (idempotent, 25 invented entries) before workers start; matcher cache refreshed every supervisor poll | The watchlist was empty on the first laptop run because seeding only ran inside the harvest flow | gap-closure pass 2, item 5 |
| C12 | **No `.exe`; a Python launcher (`launch.py`) with `.bat` wrappers** that fetches portable ffmpeg, creates the venv, pins deps, fetches weights, probes, seeds, starts API + workers detached with log files | PyInstaller-bundling Paddle + ONNX + OpenCV + ffmpeg is a multi-hour fragile build with no scoring value | LAUNCHERS entry |
| C13 | *(superseded 21 Sep by the cut order in `docs/tasks.md`)* **Cut order when time runs short** (verbatim `PLAN.md`): 1 Pipeline 3 · 2 intrusion detection · 3 WHEP · 4 object-detection surfacing · 5 bulk-CSV import *screen* (keep the endpoint) · 6 fuzzy matching. **Never cut:** registry, GIS map, live wall, ANPR, sightings search, watchlist, alerts, route reconstruction, any submission document | The scored moment outranks everything | `docs/reference/old-build/PLAN.md` |
| C14 | **Health of active RTSP cameras is judged by local tee freshness, not by probing the CDN** | A CDN probe flipped 29/30 cameras offline and emptied the active set | gap-closure pass 2, item 7 |
| C15 | **Alert SSE in the API tails the alerts table** (2 s poll) rather than an in-process bus | Alerts fire in the worker process | gap-closure pass 2, item 6 |
| C16 | **Every number in the HLD labelled `[measured]` or `[model]`** | One caught fabrication discounts the whole submission | CLAUDE.md rule 8 |

## 2. Made on 20 September 2026 for the fresh build

| # | Decision | Why |
|---|---|---|
| F1 | **Fresh, structured rebuild in this repo; the previous repo is read-only reference.** Knowledge is carried over verbatim (this `docs/` set); code is rewritten to the corrected contract | Adi's call after the structure review: the old docs pointed at folders that did not exist, rules conflicted with the enhancement plan, and the contract had drifted from the code |
| F2 | **Layout: `backend/` (API, storage, alerting, route, reports), `ml/` (ingest, detection, OCR, tracking, zones — the worker process), `frontend/` (React)**, each with its own `CLAUDE.md`. Where the two Python layers share code (config, DB access, plate normalisation, the matcher), see open item O2 | Adi's template; matches the two-process shape |
| F3 | **Git from the first commit, a tag per milestone, a regression test for every bug fixed** | The review found three regressions of things that once worked; the old repo had no git and no tests |
| F4 | *(auth model superseded 22 Sep by F41 — keys stay for scripts; people log in)* **Auth from day one**: `X-API-Key` + roles `viewer`/`admin`, crops behind auth, an append-only audit table | Review defect D2; the PRD's "no auth unless P1–P4 complete" is withdrawn |
| F5 | **No bulk download of the sandbox footage.** Test replay uses **Adi's own footage** (a phone clip of a road is enough) served by a local RTSP/HLS replay; real-feed testing stays on the live sandbox | CLAUDE.md rule 6 and the organisers' "don't plan around obtaining copies of the footage"; the review's Phase 0.1 conflicted with both |
| F6 | **Python 3.13.9 (the installed Anaconda), pinned requirements with hashes, checksum-pinned ffmpeg zip and model weights** | Validated wheels; review defect D14 |
| F7 | **Every timestamp carries its clock; every row carries provenance** (`live | harvest | demo`) — see `docs/api.md` B6 | Review defect D3 breaks the scored route |
| F8 | **The submitted deliverables are carried over as the base and corrected, not rewritten** — see `docs/architecture.md` Part C for the claim-by-claim list | Three of seven evaluation areas are documentation; the architecture is unchanged |
| F9 | **Second "system" for the Model 2 viewer requirement: a local camera (phone/webcam) published over RTSP and onboarded through the new camera form** | Model 2 requires feeds from at least two different systems; the sandbox is one system |
| F10 | **Pipeline 3 is built only after the route, alerts and both onboarding screens work**; until then it stays "described, validated separately" | Cut order C13; the deck already presents it that way honestly |
| F11 | **Stack confirmed** (closes O1): FastAPI + uvicorn, SQLite WAL, React 18 + Vite + Leaflet + hls.js, YOLOX-S on ONNX Runtime (DirectML), PaddleOCR PP-OCRv5_mobile, MOG2, ffmpeg, mediamtx (MIT) for local replay/second system | Every piece validated on this laptop; any swap would cost a day and re-open licensing |
| F12 | **Python layout** (closes O2): top-level packages `backend/` and `ml/`, run from the repo root (`python -m backend.app`, `python -m ml`); shared code in `backend/core/` (config, logging, db + migrations, timeline, plates, matcher, alerts, cdn_session); `ml` imports `backend.core`; `tests/` at the root with `pytest.ini` `pythonpath = .`; `frontend/` is the React app; `scripts/` holds one-off helpers | Two processes, one codebase, no packaging step |
| F13 | **Time base** (closes O3): every sighting/event carries `seen_at` (= stream-time instant), `wall_time`, `clock_source` ∈ `rtsp-live \| hls-vod \| harvest \| demo \| replay`, `provenance` ∈ `live \| harvest \| demo`. RTSP: `seen_at = wall_time = pull_start + PTS`. HLS-VOD/harvest: `seen_at = RECORDING_EPOCH (IST-anchored) + position`. Route groups stops by `clock_source`, computes speed only within a group, returns `warnings[]` when groups mix | P7 defect D3 — the scored route breaks when clocks mix |
| F14 | **Tracker** (closes O4): greedy IoU + centre tracker matching on vehicle superclass with PTS-delta velocity; described truthfully in the HLD ("IoU tracker", not ByteTrack). Kalman/BYTE-style only after a measured ID-switch reduction, never by copying ByteTrack's filter | Honesty of claims; GPL lineage |
| F15 | **Harvest** (closes O5): time-boxed session S4.2 (3 h; hard stop re-based by F22 to **Thu 24 20:00**); Junagadh (cam06/09/10) and Bilimora (cam26/27/28) clusters, daylight tail of the recording, gentle pacing; the demo vehicle stays the guaranteed route path | CDN rate-limits; the scored moment must not depend on it |
| F16 | **Pipeline 3** (closes O6): built only if S4.2 finished on Thu 24 **and** S5.1–S5.3 are done by Fri 25 noon (re-based by F22); cut candidate #1 | F10 |
| F17 | *(superseded 22 Sep by F42 — the demo is hosted)* **No hosted URL** for the judges (closes O7); local demo only. Revisit on Sat 26 only if everything else is done (would need a tunnel + the API key story) | Optional deliverable; time |
| F18 | **Measurements** (closes O9): `launch.py measure` — after ≥ 10 min warm-up, a 10-minute window sampling per-camera sustained fps, detections/min, motion-skip rate, plate-read rate (full reads ÷ vehicle tracks), peak VRAM (`nvidia-smi`), peak RSS of both processes; written to `data/measurements/` and pasted into progress.md as `[measured]` | HLD's blank anchor rows; rule 8 |
| F19 | **Second system** (detail of F9): `mediamtx` on the laptop publishing Adi's own clip (or the webcam via `dshow`) at `rtsp://127.0.0.1:8554/stream/local01`; registered through the add-camera form as `local01`, `transport='rtsp'`, plain URL, `source='manual'`, `fps_tier='active'` | Model 2's "two different systems"; also demonstrates manual onboarding live |
| F20 | **Test footage**: a synthetic clip generated with ffmpeg lavfi (`tests/fixtures/make_synthetic.py`) for frame-source and soak tests; real sandbox frames from `deliverables/deck/img/` for detector tests; a PIL-rendered plate for OCR tests; Adi's own footage only for the demo second system. **No sandbox downloads** | F5; tests must not depend on the CDN |
| F21 | **Alerts fire on exact and ambiguity matches only by default**; fuzzy (confusion-weighted distance ≤ 1.0, both sides structurally `full` — `docs/api.md` §6) alerts only when `SENTINEL_ALERT_ON_FUZZY=true`; partial reads never fuzzy-match; fuzzy candidates are always shown on the route, flagged | P7 D6: plain Levenshtein merges neighbouring registrations (`…1234` vs `…1235`); a false alert on a neighbour's car is worse than a missed fuzzy hit |
| F22 | **Calendar re-based (21 Sep 00:45):** sessions start Mon 21 (the Sunday slots were never run); GATE A′ Tue, GATE B and C Thu 24 (C at 20:00), **GATE D Fri 25 09:00**, code freeze Fri, soak + rehearsal Sat, submit Sun 27, Mon 28 buffer. If S0.3 shows the 28th is a live evaluation, Fri 25 is still the last build day (48-hour rule) | Nothing had run by Sunday midnight; ~47 h of session budget must fit Mon–Fri with the documents never squeezed |
| F23 | *(extended 22 Sep by F41 — a session cookie from a login is the browser's transport; this row still governs script/API-key access)* **Auth transport:** `X-API-Key` header everywhere; additionally a `sentinel_key` cookie (`SameSite=Strict`, `HttpOnly`, set by `POST /api/session`) is accepted for **GET** on `/crops/*`, `/api/hls/*` and `/api/alerts/stream` only. Mutations always need the header **and** admin. Open paths: `/`, `/assets/*`, `/api/health` (`/docs` and `/openapi.json` were open here; F41 moved them behind the login now that the platform is public) | `<img src>`, hls.js and `EventSource` cannot send custom headers; a cookie that never authorises a mutation keeps the CSRF hole closed |
| F24 | **Dependencies pinned to exact versions** (the validated cp313 set from the old repo: `paddlepaddle==3.3.1`, `paddleocr==3.7.0`, the rest from a clean `pip freeze`); hash-locking deferred past the 28th (amends F6). `python-dotenv` loads the repo-root `.env` in `config.py` (`override=False`) | Reproducible installs today; hash lockfiles cost a pip-tools setup the deadline cannot afford |
| F25 | **ffmpeg/ffprobe are resolved by `backend.core.config.ffmpeg()`/`ffprobe()`**: `SENTINEL_FFMPEG_DIR` → PATH → clear error; default points at the old repo's portable build. The launcher fetches the BtbN build only if neither resolves. **Every downloaded binary's SHA-256 lives in the committed root `CHECKSUMS.txt`** (mediamtx, ffmpeg zip, `yolox_s.onnx`); `models/` and `tools/` stay ignored (amends C12, F6) | ffmpeg is not on the laptop's PATH; a checksum file inside an ignored folder is never committed |
| F26 | **Provenance enum is `live \| harvest \| demo \| test`**; replay-transport reads are `test`; the 10-minute soak runs on a separate `data/soak.db` so synthetic cameras and reads never enter the working database (amends F13 and CLAUDE.md rule 9) | Synthetic rows must not satisfy "≥ 1 live row" or appear on the map |
| F27 | **Zone alerts are `alerts` rows** with `kind='zone'`, `event_id` set and `sighting_id`/`watchlist_id`/`plate` null; the SSE tailer reads `alerts` only. `match_type` ∈ `exact \| ambiguity \| fuzzy \| none`, `match_distance REAL`. Cooldowns derive from the last matching `alerts` row (same `clock_source`), never from memory (amends api.md B7/B8) | One stream, one table; a purge must reset the cooldown (regression D4) |
| F28 | **Frontend versions:** Node 20.19+; `npm create vite@6` React template with `react@18.3.1`, `react-dom@18.3.1`, `react-router-dom@6`, `leaflet@1.9.4`, `react-leaflet@4.2.1`, `hls.js@1` (amends C4) | Current create-vite scaffolds React 19; react-leaflet 5 needs React 19, v4 needs 18 — the old, validated combination is kept |
| F29 | **`SENTINEL_RECORDING_EPOCH` default `2026-06-13T21:00:00+05:30`, `LOOP_SECONDS` 43200** — a demo constant anchoring the recordings' burned-in clock for display and harvest alignment, labelled as such, never presented as a measurement | The findings give two recording days and a ~3 h offset; one declared anchor beats an implicit one |
| F30 | **`frontend/dist` stays ignored** (closes O11); S5.5 zips the build into `deliverables/frontend-dist.zip` on the submission tag; `v2.0-submission` is tagged on `main` | Definition of done says the tag is on `main`; a zip is reviewable, a committed build tree is not |
| F31 | **Health checker is built in S3.1b** as an API background task: active RTSP cameras by tee freshness, the rest by paced RTSP `ffprobe`, never by probing the CDN (implements FR-1.5 and C14) | The previous plan configured it but no task built it |
| F32 | **S1.3 and S3.1 are split into a/b sessions; the demo seeder moves to S3.1a** so S3.3's screen tests can use it | Both were too large for one context; S3.3 needed the seeder before S3.4 |
| F33 | **Shell convention:** Claude Code's shell on Windows is Git Bash; commands are written in bash form with explicit interpreters (`.venv/Scripts/python -m …`; plain `python` = Anaconda before the venv exists); env vars inline, never `activate`/`set` | Nothing persists between tool calls; `activate` and `set` silently do nothing there |
| F34 | **All database writes from the worker process go through one writer thread**; sightings and alerts are awaited jobs (persist-before-match, persist-before-broadcast preserved), events are async batches; `alerts` carries `zone_id` so zone cooldowns key on it | A single writer cannot hit its own locks; the old build's per-frame lock crashes cannot recur |
| F35 | **`seed_registry` upserts every catalogue id from the committed `data/cameras_raw.json`** before applying the seed, so any fresh database has 30 rows without the sandbox; `--add` and `--replay` options create manual/local and replay cameras; the probe writes `data/probe_results_<date>.json` and never overwrites the 14 Sep evidence file | Tests and the soak must not depend on the CDN or the gateway |
| F36 | **Replay stream_time is `pull_start + index/fps`, continuous across loops**; the wrap tick resets trackers and the motion gate only. The backoff logs `attempt`, `base`, `delay` and resets after 30 s of health; tests assert on `base`, never on the jittered value | A clock that jumps at every loop would break dedupe and cooldowns in the soak; jitter makes ordering assertions flaky |
| F37 | **Pins are copied from the old `.venv`, not from a fresh resolve** (amends F24): `fastapi==0.141.1`, `uvicorn[standard]==0.53.0`, `python-multipart==0.0.32`, `python-dotenv==1.2.3`, `httpx==0.28.1`, `pydantic==2.13.5`, `numpy==2.3.5`, `opencv-contrib-python==4.10.0.84`, `Pillow==12.3.0`, `cryptography==50.0.1`, `psutil==7.2.2`, `onnxruntime-directml==1.24.4` (win32 marker), `paddlepaddle==3.3.1`, `paddleocr==3.7.0`, `paddlex==3.7.2` — the list as written in `docs/tasks.md` S1.1 is the one to install. **Exactly one OpenCV distribution — `opencv-contrib-python` — and never the plain `onnxruntime` wheel on Windows.** S1.1's acceptance runs `pip check` and asserts the two package names | The old venv had `opencv-python 5.0.0.93` (from its unpinned `requirements.txt`) and `opencv-contrib-python 4.10.0.84` (pulled in by paddlex) unpacked over the same `cv2/` folder, 49 files in common; the contrib wheel, installed last, is what actually ran. A fresh resolve would repeat the clash or pick versions nobody has run on this laptop |
| F38 | **Claude Code sessions run under the committed `.claude/settings.json`** (seeded as `scripts/claude-settings.json` by the v2.2 review and moved into place by S0.2): read access to `D:\projects\Sentinel_Repo` without prompting, edits there denied, `.env` unreadable and uncreatable by any session (Adi owns the file), Bash default timeout 5 min and ceiling 60 min for the soaks. Steps a session physically cannot do are marked `[Adi]` inside the task — the `.env` copy before S0.4 and the two network pulls (S2.2, S6.1: cutting the network also cuts Claude Code off). The previous build's constitution is archived as `docs/reference/old-build/CONSTITUTION.md`, never as a `CLAUDE.md` | Dozens of *Read first* pointers go into the old repo — a prompt on each would stall every session; and Claude Code loads any `CLAUDE.md` in a folder it reads from, so a stale rulebook ("today is 13 September", "no authentication beyond a demo role") must not carry that name |
| F39 | **The watchlist cache excludes rows past `expires_at` at refresh time** (`backend/core/matcher.py`, S1.2): `active = 1 AND (expires_at IS NULL OR expires_at > now)`. An expired entry never matches and never alerts | The schema adds `expires_at` without stating its runtime meaning; alerting on a lapsed entry is the worse failure, so the conservative reading is enforced in code |
| F40 | **Plate grammar precedence** (`plate_like`, S1.2): uncoerced full → uncoerced structural prefix (partial) → coercion-dependent full → None. A read that is full only via letter→digit coercion but also parses as a truncated plate as-is (`GJ05JB432`) is **partial** — coercion salvages OCR confusions, it never rewrites a clean truncated read into a different registration | §6's coercion clause and prefix clause conflict on exactly this input; the S1.2 acceptance table pins it as partial, and inventing digits into a truncated read is the unsafe direction |
| F41 | **People log in; keys are for scripts** (supersedes F4's transport, extends F23). Schema v2 adds `users` (username, `password_hash` via `hashlib.scrypt`, role, active, created_at, last_login) and `sessions` (opaque id, user, issued_at, expires_at, revoked). `POST /api/auth/login` sets `sentinel_session` (`HttpOnly`, `Secure`, `SameSite=Strict`, 8 h); `POST /api/auth/logout` and `GET /api/auth/me` complete it. The session cookie authorises **everything the role allows**, including `<img>` crops, hls.js and the alert `EventSource`, because the UI and the API share one origin. `X-API-Key` keeps working for scripts and tools. Roles: `viewer` (read), `evaluator` (read + acknowledge + watchlist add/remove + reports + onboarding form), `admin` (everything incl. users and zones). `audit.actor` becomes the username; login success, failure and logout are audited. Accounts are created by `python -m backend.tools.users add` — never in `.env`, never in the repo | The screening committee gets a URL with test credentials (portal Step 5); shared API keys cannot name a person in an audit trail, and a key dialog is not a login |
| F42 | **The demo is hosted** (reverses F17): the platform runs on the laptop and is published over a **tunnel** — Tailscale Funnel by default (free on every plan, stable `https://<machine>.<tailnet>.ts.net`, TLS terminated on the device, `--bg` survives reboots), or a Cloudflare **named** tunnel if a domain is already owned. Cloudflare quick tunnels are refused (no Server-Sent Events, random URL, no SLA) and ngrok free is refused (1 GB and 20,000 requests a month). Only the API port is published; mediamtx and Vite stay bound to `127.0.0.1` | The feeds, the GPU and the one-pull-per-camera rule live on the laptop, so hosting means publishing the laptop safely; evaluation area 07 counts a dead link or a failed credential against the submission |
| F43 | **Ground truth comes from our own footage**: two or three clips of one real vehicle filmed at real, separate locations, published as `local01`–`local03` through mediamtx with those locations' coordinates, produce a **real** watchlist hit and a **real** multi-camera route from live ANPR. The injected demo vehicle stays as labelled insurance only | No real plate has ever been read on two sandbox cameras (`docs/sandbox-findings.md` §4); a route built only from injected rows cannot be called a working demonstration |
| F44 | **The RTSP stall timeout is `-timeout`, not `-rw_timeout`**, and the watchdog arms when the child is spawned, not at the first frame | Measured: against a server that accepts the connection and never answers, `-rw_timeout 3000000` hung until killed at 25 s while `-timeout 3000000` gave up after 3.2 s; a pull that never delivers a first frame is exactly how cam07 and cam25 behaved |
| F45 | **The catalogue reader accepts both shapes**: the sandbox's `cameras.json` (`{id, name}`) and the Integrator's Guide's `GET /api/ingest` (id, location, codec, live status, stream properties, all three URLs). Whatever the catalogue supplies is used verbatim; `data/camera_seed.csv` fills only what it does not | "The catalogue is the contract, the URL pattern is not" — the feeds provided for the demo may publish the guide's shape rather than the sandbox's |
| F46 | **The platform tells an evaluator what it is showing**: a first screen after login naming what to click and which plates to try, a feed-status strip that distinguishes "no traffic" from "feed down" from "pipeline dead", and provenance badges (`live \| demo \| test`) wherever rows are displayed | A judge with credentials and no guidance sees a dashboard and leaves; and an unlabelled demo row read as a live one is the fabrication risk the whole submission is judged against |
| F47 | **The in-stream loop cut (feed rule 8) is not detected on RTSP** — `restart` fires on reconnect only; the shared `SceneCutDetector` is left wired only where content diff is reliable (replay uses PTS-wrap instead). A luminance-baseline detector is deferred | Measured on live cam06 (S2.2): ordinary 3 fps traffic reaches a whole-frame mean-abs-diff of 138 (p90=46, p95=79), overlapping any real cut, so a diff threshold false-fires ~9 % of frames. A restart storm resets the tracker and breaks route reconstruction — far worse than missing a self-healing cut that occurs ~twice per 12 h loop |
| F48 | **mediamtx runs bound to `127.0.0.1` with every service but RTSP/TCP disabled** (`scripts/replay_publish.py`): RTMP, HLS, WebRTC, SRT, API, metrics, pprof, playback and **MoQ** off via `MTX_*` env overrides; the release zip is SHA-256-pinned in `CHECKSUMS.txt` and re-verified every run, and `start_mediamtx` fails loudly if its child loses the port bind | Root rule 11 (nothing but the API port leaves localhost); mediamtx 1.21 binds MoQ on `0.0.0.0` by default, which triggered a Windows Firewall prompt; a leaked prior instance holding 8554 otherwise masks itself and starves pulls to zero frames |

## 3. Design rules from the review of the previous build

The review (`docs/reference/old-build/P7-enhancements.md` §0) found 15 defects, each verified against code. The contract-level ones are already in `docs/api.md` Part B (B6–B14). The rest become build rules here, grouped by layer.

**Ingestion (ml/)**
- Any looping source resets its "last yielded PTS" on wrap or resync, and the wrap wait must not busy-loop the CDN (D1). Test across two simulated wraps on the local replay before trusting it.
- Every pull has a **stall watchdog**: no frame for N seconds → kill the ffmpeg child → existing jittered backoff (D9). Add **`-timeout`** on RTSP — **not** `-rw_timeout`, which was measured to have no effect on an RTSP connection (decision F44) — and arm the watchdog when the child is spawned.
- Motion gate, object-event throttle and trackers reset on `stream_restart` (D12).
- `roi_json` is wired end to end: mask detection and the motion gate per camera; the caption band becomes a per-camera exclusion zone (D12, A4).
- OCR uses **track-level consensus voting**, never a first-read latch; per-track OCR budget stays (D10, A1). The DirectML detector session is locked; CPU ONNX sessions are thread-safe and need no global lock.
- Tracker matches on vehicle superclass so car↔truck flips do not fragment tracks; per-class NMS in the detector (A3).
- Plate grammar covers BH-series and a state-code whitelist, in one module (D11, A2).

**Backend**
- Health checker probes first, writes after, in one short transaction — never a write transaction held across network probes (D9).
- Single writer thread with `BEGIN IMMEDIATE` for the worker's DB writes; `busy_timeout=30 s`, `synchronous=NORMAL` under WAL; event writes batched (the review's capacity item C4, not decision C4).
- `stop()` kills only its own child processes (psutil process tree), never every `ffmpeg.exe` on the machine; WMIC is dead on Windows 11 24H2 (D9).
- Retention: configurable TTL purge per data class (sightings, non-hit crops shorter, watchlist-hit evidence longer under a case reference).

**Frontend**
- One `formatTs()` in IST via `Intl.DateTimeFormat`; no raw UTC slices (D13).
- Every fetch checks `r.ok`; failures are visible in a connection-status strip — never `.catch(() => {})` (D13).
- The severity map covers every value incl. `critical`; the alerts list is capped; OSM attribution is on (ODbL) (D13).
- Zone saves report the real result; zone edits reach the workers without a restart (D12, D13).

**Ops and integrity**
- Every process writes a rotating log file under `data/logs/`; the launcher polls the port and fails loudly (D9).
- Requirements pinned with environment markers (`onnxruntime-directml; sys_platform == "win32"`) and a hash-locked lockfile; SHA-256 pins for the ffmpeg zip and `yolox_s.onnx` (D14).
- No credential in process `argv` (D14).
- Claims match code; `[measured]` only after the measurement ran (D15).

**Measured verdicts to reuse, not re-derive** (all measured on this laptop, per the review §6): NVDEC decode works on the GTX 1650 (driver 512.78) with a ~6× decode-CPU cut per camera and ~129 MB VRAM per session, but costs ~200 MB system RAM per camera; batched YOLOX-S is 3.0× at batch 4 but DirectML needs `add_free_dimension_override_by_name('batch', 4)` or it crashes; **FP16 under DirectML is 1.5–1.8× slower** (refuted); `fast-plate-ocr` is 9.5 ms/crop on CPU but India is absent from its training regions (side-by-side gate mandatory); one OSRM `/table` call returns the 30×30 road matrix in 1.28 s with median circuity 1.19 and p90 1.92 (a flat 1.3 factor is refuted). Recipes: `docs/reference/old-build/P7-enhancements.md` Appendix.

## 4. Do-NOT-do list *(verbatim, P7-enhancements.md §9)*

- **FP16 under DirectML** — measured slower; production-HLD material only.
- **TypeScript migration of the UI** — high effort, zero user-visible value solo; generate types for the new data layer only.
- **Postgres/PostGIS port now** — SQLite + indexes + single-writer carries a 10–30 camera pilot comfortably; build only the seam.
- **Full Gujarati i18n now** — the actual operator pain is the UTC display bug (fixed in Phase 1); translate once a pilot commits.
- **YOLOX-Tiny** — capacity is no longer the constraint after C1/C2; the small-object accuracy cost lands exactly where ANPR hurts.
- **Copying ByteTrack's `kalman_filter.py` or using `open-image-models` YOLOv9 plate weights** — GPL lineage in a state-owned stack.
- **`leaflet.offline` pre-seeding of OSM tiles** — prohibited by the OSMF tile policy.
- **Training or fine-tuning any model** — standing rule; it is also why LPRNet-class (Chinese-plate) candidates are rejected.
- **Refactor-for-elegance of working modules; a test suite for its own sake** — the constitution's rules still hold.

## 5. Open

Settled on 20 Sep by the plan (`docs/tasks.md`): O1 → F11, O2 → F12, O3 → F13, O4 → F14, O5 → F15, O6 → F16, O7 → F17, O9 → F18; on 21 Sep by the review: O11 → F30. Still open:

| # | Question | Default if not decided | Owner / when |
|---|---|---|---|
| O8 | Entry category (1 vs 2). **The milestone half is answered** (portal, 22 Sep): 28 Sep is the submission and shortlisting date; the on-site hackathon where the plate is handed over is 12–13 Oct at i-Hub (`docs/brief.md` §0) | Category 1 | Adi, task S0.3 (category only) |
| O10 | Department label and ownership for the local second-system camera `local01` | department `Municipal`, `ownership='private'` with a consent note | Adi, when adding it in S3.4 |
